extension DoubleExtension on double {
  /// Returns the double value as a string.
  String get stringValue => toString();

  /// Returns the double value as an integer.
  int get intValue => toInt();

  /// Returns the double value as a number.
  num get numValue => toDouble();
}
